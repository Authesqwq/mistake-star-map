{
  "name": "mistake-star-map",
  "version": "1.0.0",
  "private": true,
  "description": "错题星图 - AI 错题复练与知识点掌握系统",
  "scripts": {
    "install:all": "npm install && cd client && npm install && cd ../server && npm install",
    "dev": "concurrently -n client,server -c blue,green \"npm run dev:client\" \"npm run dev:server\"",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "build": "npm run build:client && npm run build:server",
    "build:client": "cd client && npm run build",
    "build:server": "cd server && npm run build",
    "validate:diagnosis-schema": "cd server && npm run validate:diagnosis-schema",
    "eval:diagnosis": "cd server && npm run eval:diagnosis",
    "eval:diagnosis:fallback": "cd server && npm run eval:diagnosis:fallback",
    "eval:diagnosis:live": "cd server && npm run eval:diagnosis:live"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
